<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itye.mall.mapper.OperationLogMapper">
    <sql id="columns">id, admin_id, action, target_type, target_id, ip_address, user_agent, detail_json, created_at</sql>
    <select id="selectById" resultType="com.itye.mall.entity.OperationLog">SELECT <include refid="columns" /> FROM operation_logs WHERE id = #{id}</select>
    <select id="selectAll" resultType="com.itye.mall.entity.OperationLog">SELECT <include refid="columns" /> FROM operation_logs</select>
    <select id="selectPage" resultType="com.itye.mall.entity.OperationLog">
        SELECT <include refid="columns" />
        FROM operation_logs
        WHERE 1 = 1
        <if test="adminId != null">AND admin_id = #{adminId}</if>
        <if test="action != null and action != ''">AND action = #{action}</if>
        ORDER BY id DESC
        LIMIT #{offset}, #{pageSize}
    </select>
    <select id="count" resultType="long">
        SELECT COUNT(*)
        FROM operation_logs
        WHERE 1 = 1
        <if test="adminId != null">AND admin_id = #{adminId}</if>
        <if test="action != null and action != ''">AND action = #{action}</if>
    </select>
    <insert id="insert" parameterType="com.itye.mall.entity.OperationLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO operation_logs
        <trim prefix="(" suffix=")" suffixOverrides=","><if test="adminId != null">admin_id,</if><if test="action != null">action,</if><if test="targetType != null">target_type,</if><if test="targetId != null">target_id,</if><if test="ipAddress != null">ip_address,</if><if test="userAgent != null">user_agent,</if><if test="detailJson != null">detail_json,</if><if test="createdAt != null">created_at,</if></trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=","><if test="adminId != null">#{adminId},</if><if test="action != null">#{action},</if><if test="targetType != null">#{targetType},</if><if test="targetId != null">#{targetId},</if><if test="ipAddress != null">#{ipAddress},</if><if test="userAgent != null">#{userAgent},</if><if test="detailJson != null">#{detailJson},</if><if test="createdAt != null">#{createdAt},</if></trim>
    </insert>
    <update id="updateById" parameterType="com.itye.mall.entity.OperationLog">
        UPDATE operation_logs
        <set><if test="adminId != null">admin_id = #{adminId},</if><if test="action != null">action = #{action},</if><if test="targetType != null">target_type = #{targetType},</if><if test="targetId != null">target_id = #{targetId},</if><if test="ipAddress != null">ip_address = #{ipAddress},</if><if test="userAgent != null">user_agent = #{userAgent},</if><if test="detailJson != null">detail_json = #{detailJson},</if><if test="createdAt != null">created_at = #{createdAt},</if></set>
        WHERE id = #{id}
    </update>
    <delete id="deleteById">DELETE FROM operation_logs WHERE id = #{id}</delete>
</mapper>
