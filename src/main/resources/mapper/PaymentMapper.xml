<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itye.mall.mapper.PaymentMapper">
    <sql id="columns">id, payment_no, order_id, order_no, user_id, channel, amount, status, transaction_id, paid_at, callback_payload, created_at, updated_at</sql>
    <select id="selectById" resultType="com.itye.mall.entity.Payment">SELECT <include refid="columns" /> FROM payments WHERE id = #{id}</select>
    <select id="selectAll" resultType="com.itye.mall.entity.Payment">SELECT <include refid="columns" /> FROM payments</select>
    <select id="selectLatestByOrderId" resultType="com.itye.mall.entity.Payment">
        SELECT <include refid="columns" /> FROM payments WHERE order_id = #{orderId} ORDER BY id DESC LIMIT 1
    </select>
    <insert id="insert" parameterType="com.itye.mall.entity.Payment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO payments
        <trim prefix="(" suffix=")" suffixOverrides=","><if test="paymentNo != null">payment_no,</if><if test="orderId != null">order_id,</if><if test="orderNo != null">order_no,</if><if test="userId != null">user_id,</if><if test="channel != null">channel,</if><if test="amount != null">amount,</if><if test="status != null">status,</if><if test="transactionId != null">transaction_id,</if><if test="paidAt != null">paid_at,</if><if test="callbackPayload != null">callback_payload,</if><if test="createdAt != null">created_at,</if><if test="updatedAt != null">updated_at,</if></trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=","><if test="paymentNo != null">#{paymentNo},</if><if test="orderId != null">#{orderId},</if><if test="orderNo != null">#{orderNo},</if><if test="userId != null">#{userId},</if><if test="channel != null">#{channel},</if><if test="amount != null">#{amount},</if><if test="status != null">#{status},</if><if test="transactionId != null">#{transactionId},</if><if test="paidAt != null">#{paidAt},</if><if test="callbackPayload != null">#{callbackPayload},</if><if test="createdAt != null">#{createdAt},</if><if test="updatedAt != null">#{updatedAt},</if></trim>
    </insert>
    <update id="updateById" parameterType="com.itye.mall.entity.Payment">
        UPDATE payments
        <set><if test="paymentNo != null">payment_no = #{paymentNo},</if><if test="orderId != null">order_id = #{orderId},</if><if test="orderNo != null">order_no = #{orderNo},</if><if test="userId != null">user_id = #{userId},</if><if test="channel != null">channel = #{channel},</if><if test="amount != null">amount = #{amount},</if><if test="status != null">status = #{status},</if><if test="transactionId != null">transaction_id = #{transactionId},</if><if test="paidAt != null">paid_at = #{paidAt},</if><if test="callbackPayload != null">callback_payload = #{callbackPayload},</if><if test="createdAt != null">created_at = #{createdAt},</if><if test="updatedAt != null">updated_at = #{updatedAt},</if></set>
        WHERE id = #{id}
    </update>
    <delete id="deleteById">DELETE FROM payments WHERE id = #{id}</delete>
</mapper>
