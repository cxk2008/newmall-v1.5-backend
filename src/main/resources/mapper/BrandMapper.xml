<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itye.mall.mapper.BrandMapper">
    <sql id="columns">id, name, logo_url, description, sort_order, status, created_at, updated_at, deleted_at</sql>
    <select id="selectById" resultType="com.itye.mall.entity.Brand">SELECT <include refid="columns" /> FROM brands WHERE id = #{id}</select>
    <select id="selectAll" resultType="com.itye.mall.entity.Brand">SELECT <include refid="columns" /> FROM brands</select>
    <select id="selectAdminPage" resultType="com.itye.mall.entity.Brand">
        SELECT <include refid="columns" />
        FROM brands
        WHERE deleted_at IS NULL
        <if test="keyword != null and keyword != ''">AND name LIKE CONCAT('%', #{keyword}, '%')</if>
        <if test="status != null">AND status = #{status}</if>
        ORDER BY sort_order ASC, id DESC
        LIMIT #{offset}, #{pageSize}
    </select>
    <select id="countAdmin" resultType="long">
        SELECT COUNT(*)
        FROM brands
        WHERE deleted_at IS NULL
        <if test="keyword != null and keyword != ''">AND name LIKE CONCAT('%', #{keyword}, '%')</if>
        <if test="status != null">AND status = #{status}</if>
    </select>
    <insert id="insert" parameterType="com.itye.mall.entity.Brand" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO brands
        <trim prefix="(" suffix=")" suffixOverrides=","><if test="name != null">name,</if><if test="logoUrl != null">logo_url,</if><if test="description != null">description,</if><if test="sortOrder != null">sort_order,</if><if test="status != null">status,</if><if test="createdAt != null">created_at,</if><if test="updatedAt != null">updated_at,</if><if test="deletedAt != null">deleted_at,</if></trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=","><if test="name != null">#{name},</if><if test="logoUrl != null">#{logoUrl},</if><if test="description != null">#{description},</if><if test="sortOrder != null">#{sortOrder},</if><if test="status != null">#{status},</if><if test="createdAt != null">#{createdAt},</if><if test="updatedAt != null">#{updatedAt},</if><if test="deletedAt != null">#{deletedAt},</if></trim>
    </insert>
    <update id="updateById" parameterType="com.itye.mall.entity.Brand">
        UPDATE brands
        <set><if test="name != null">name = #{name},</if><if test="logoUrl != null">logo_url = #{logoUrl},</if><if test="description != null">description = #{description},</if><if test="sortOrder != null">sort_order = #{sortOrder},</if><if test="status != null">status = #{status},</if><if test="createdAt != null">created_at = #{createdAt},</if><if test="updatedAt != null">updated_at = #{updatedAt},</if><if test="deletedAt != null">deleted_at = #{deletedAt},</if></set>
        WHERE id = #{id}
    </update>
    <delete id="deleteById">UPDATE brands SET deleted_at = NOW(), updated_at = NOW() WHERE id = #{id}</delete>
</mapper>
