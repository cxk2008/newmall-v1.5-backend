<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itye.mall.mapper.ProductReviewMapper">
    <sql id="columns">id, user_id, order_id, order_item_id, product_id, sku_id, rating, content, images_json, is_anonymous, status, reply_content, replied_at, created_at, updated_at</sql>
    <select id="selectById" resultType="com.itye.mall.entity.ProductReview">SELECT <include refid="columns" /> FROM product_reviews WHERE id = #{id}</select>
    <select id="selectAll" resultType="com.itye.mall.entity.ProductReview">SELECT <include refid="columns" /> FROM product_reviews</select>
    <select id="selectByOrderItemId" resultType="com.itye.mall.entity.ProductReview">
        SELECT <include refid="columns" /> FROM product_reviews WHERE order_item_id = #{orderItemId} LIMIT 1
    </select>
    <select id="selectByProductIdPage" resultType="com.itye.mall.entity.ProductReview">
        SELECT <include refid="columns" />
        FROM product_reviews
        WHERE product_id = #{productId} AND status = 1
        ORDER BY id DESC
        LIMIT #{offset}, #{pageSize}
    </select>
    <select id="countByProductId" resultType="long">
        SELECT COUNT(*) FROM product_reviews WHERE product_id = #{productId} AND status = 1
    </select>
    <select id="selectByOrderId" resultType="com.itye.mall.entity.ProductReview">
        SELECT <include refid="columns" /> FROM product_reviews WHERE order_id = #{orderId} ORDER BY id DESC
    </select>
    <insert id="insert" parameterType="com.itye.mall.entity.ProductReview" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product_reviews
        <trim prefix="(" suffix=")" suffixOverrides=","><if test="userId != null">user_id,</if><if test="orderId != null">order_id,</if><if test="orderItemId != null">order_item_id,</if><if test="productId != null">product_id,</if><if test="skuId != null">sku_id,</if><if test="rating != null">rating,</if><if test="content != null">content,</if><if test="imagesJson != null">images_json,</if><if test="isAnonymous != null">is_anonymous,</if><if test="status != null">status,</if><if test="replyContent != null">reply_content,</if><if test="repliedAt != null">replied_at,</if><if test="createdAt != null">created_at,</if><if test="updatedAt != null">updated_at,</if></trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=","><if test="userId != null">#{userId},</if><if test="orderId != null">#{orderId},</if><if test="orderItemId != null">#{orderItemId},</if><if test="productId != null">#{productId},</if><if test="skuId != null">#{skuId},</if><if test="rating != null">#{rating},</if><if test="content != null">#{content},</if><if test="imagesJson != null">#{imagesJson},</if><if test="isAnonymous != null">#{isAnonymous},</if><if test="status != null">#{status},</if><if test="replyContent != null">#{replyContent},</if><if test="repliedAt != null">#{repliedAt},</if><if test="createdAt != null">#{createdAt},</if><if test="updatedAt != null">#{updatedAt},</if></trim>
    </insert>
    <update id="updateById" parameterType="com.itye.mall.entity.ProductReview">
        UPDATE product_reviews
        <set><if test="userId != null">user_id = #{userId},</if><if test="orderId != null">order_id = #{orderId},</if><if test="orderItemId != null">order_item_id = #{orderItemId},</if><if test="productId != null">product_id = #{productId},</if><if test="skuId != null">sku_id = #{skuId},</if><if test="rating != null">rating = #{rating},</if><if test="content != null">content = #{content},</if><if test="imagesJson != null">images_json = #{imagesJson},</if><if test="isAnonymous != null">is_anonymous = #{isAnonymous},</if><if test="status != null">status = #{status},</if><if test="replyContent != null">reply_content = #{replyContent},</if><if test="repliedAt != null">replied_at = #{repliedAt},</if><if test="createdAt != null">created_at = #{createdAt},</if><if test="updatedAt != null">updated_at = #{updatedAt},</if></set>
        WHERE id = #{id}
    </update>
    <delete id="deleteById">DELETE FROM product_reviews WHERE id = #{id}</delete>
</mapper>
